Some basic info :

Regular Expressions(RegEx) are an art of expressing language to search for pattrens in text and files.

Some Grouping Operations :

(a) --> pattrens that should be processed together 
[a-z] --> list of character to search for 
{1-10} --> to specify how many time a previous patterns  will repeated
| --> Or operator
.* --> AND operator